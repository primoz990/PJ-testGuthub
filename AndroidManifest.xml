<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="edu.PrimozRezek.iManager.android"
      android:versionCode="1"
      android:versionName="1.0">


    <application android:icon="@drawable/ic_menu" android:label="@string/app_name">
        <activity android:name="uraActivity" android:theme="@android:style/Theme.NoTitleBar"
                  android:label="iManager">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".vremeActivity" android:theme="@android:style/Theme.NoTitleBar"
                  android:label="iManager - Vreme">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".RssActivity" android:theme="@android:style/Theme.NoTitleBar"
                  android:label="iManager - Rss novice">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".koledarActivity" android:theme="@android:style/Theme.NoTitleBar"
                  android:label="iManager - Koledar">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".nastavitveActivity" android:theme="@android:style/Theme.NoTitleBar"
                  android:label="iManager - Nastavitve">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
    
</manifest>